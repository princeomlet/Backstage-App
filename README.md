# BACKSTAGE v2.0 - Flet Version

**Автор:** princeOMLET  
**GitHub:** https://github.com/princeomlet

Десктопное приложение для управления OBS и Telegram ботом, написанное на **Flet** (Python + Flutter).

---

## 🎨 Дизайн

Полностью воссоздан дизайн из React/TypeScript версии:
- ✅ Темная тема (#0a1628, #0d1d35, #1a1d2e, #272b3d)
- ✅ Градиентные фоны
- ✅ Акцентные цвета (Cyan #06b6d4, Emerald #10b981, Red #ef4444)
- ✅ Адаптивная структура компонентов

---

## 📁 Структура проекта

```
/flet-version/
  main.py                           # Точка входа приложения
  requirements.txt                  # Зависимости Python
  
  screens/
    login_screen.py                 # Экран авторизации
    register_screen.py              # Экран регистрации
    main_screen.py                  # Главный экран приложения
  
  components/
    app_header.py                   # Заголовок с логотипом и настройками
    status_card.py                  # Карточка статуса (OBS/SERVER)
    notifications_section.py        # Секция уведомлений
    logs_section.py                 # Логи в стиле терминала
    settings_modal.py               # Модальное окно настроек
  
  assets/                           # (TODO) Изображения и ресурсы
    logo.png                        # Логотип приложения
    pattern.png                     # Паттерн для фона
    obs_logo.png                    # Логотип OBS
```

---

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
cd flet-version
pip install -r requirements.txt
```

### 2. Запуск приложения

**Desktop версия:**
```bash
python main.py
```

**Веб-версия (для тестирования):**
```python
# В файле main.py замените последнюю строку на:
ft.app(target=main, view=ft.AppView.WEB_BROWSER, port=8000)
```

---

## 📦 Сборка .exe для Windows

### Используя PyInstaller:

```bash
pip install pyinstaller

# Базовая сборка
pyinstaller --onefile --windowed main.py

# С иконкой и дополнительными файлами
pyinstaller --onefile --windowed --icon=assets/logo.ico --add-data "assets;assets" main.py
```

### Используя Flet встроенную команду:

```bash
flet build windows
```

**Результат:** `.exe` файл появится в папке `dist/` или `build/windows/`

---

## 🎯 Для вашего IDE AI помощника

### Что уже готово (UI):
- ✅ Все экраны (Login, Register, Main)
- ✅ Все компоненты (Header, StatusCard, Notifications, Logs, Settings)
- ✅ Дизайн 1:1 с React версией
- ✅ Базовая логика переключения экранов

### Что нужно подключить (Сервер):
1. **OBS WebSocket подключение:**
   - `components/settings_modal.py` → кнопка "Подключить"
   - Использовать библиотеку `obs-websocket-py`
   
2. **Telegram Bot интеграция:**
   - Подключение к Telegram API
   - Обработка команд из Telegram
   
3. **Логи в реальном времени:**
   - `components/logs_section.py` → добавить метод `add_log(time, message, is_error)`
   
4. **Валидация форм:**
   - `screens/login_screen.py` → метод `handle_login()`
   - `screens/register_screen.py` → метод `handle_register()`

### Места для интеграции (помечены как TODO):

```python
# TODO: Добавить логотип из assets/logo.png
# TODO: Показать ошибку
# TODO: Подключение к OBS WebSocket
# TODO: Добавить метод add_log() для динамических логов
```

---

## 🎨 Цветовая палитра

```python
# Фоны
BACKGROUND_MAIN = "#1a1d2e"      # Главный фон
CARD_BG = "#1f2333"              # Фон карточек
CARD_BG_DARK = "#272b3d"         # Темный фон карточек

# Градиенты
GRADIENT_FROM = "#0a1628"        # Начало градиента
GRADIENT_VIA = "#0d1d35"         # Середина градиента
GRADIENT_TO = "#0a1628"          # Конец градиента

# Акценты
CYAN = "#06b6d4"                 # Основные действия (Start bot, кнопки)
EMERALD = "#10b981"              # Подключено (Connected)
RED = "#ef4444"                  # Отключить (Disconnect, Stop)

# Текст и границы
TEXT_WHITE = "#ffffff"
TEXT_GRAY = "#9ca3af"
TEXT_DARK_GRAY = "#6b7280"
BORDER = "#4b5563"
```

---

## 📝 Примеры использования

### Добавление логов динамически:

```python
# В logs_section.py добавьте метод:
def add_log(self, time: str, message: str, is_error: bool = False):
    """Добавить новую строку лога"""
    # Ваш IDE AI помощник может использовать этот метод
    pass
```

### Подключение к OBS:

```python
# В settings_modal.py метод toggle_obs():
import obswebsocket
from obswebsocket import obsws, requests

def connect_to_obs(self):
    """Подключение к OBS WebSocket"""
    ws = obsws(
        self.obs_address.value, 
        int(self.obs_port.value), 
        self.obs_password.value
    )
    ws.connect()
    # Дальше ваш сервер
```

---

## 🔗 Полезные ссылки

- **Flet документация:** https://flet.dev/docs/
- **OBS WebSocket:** https://github.com/obsproject/obs-websocket
- **Python Telegram Bot:** https://github.com/python-telegram-bot/python-telegram-bot

---

## 📧 Контакты

**Вопросы по дизайну:** Figma Make AI (этот файл создан мной)  
**Вопросы по серверу:** Ваш IDE AI помощник

Удачи с проектом! 🚀
